# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Ignore local environment files
.env
.env.local
.env.*.local

# Keep example env file in the repo
!.env.example

# Editor directories and files helper
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Helper: recommended commands to sync & push to origin
# 1) git status
# 2) git add .
# 3) git commit -m "message"   (skip if nothing to commit)
# 4) git fetch origin
# 5) git rebase origin/main    (resolve conflicts if any)
# 6) git push -u origin main
